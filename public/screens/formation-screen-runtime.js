(function () {
  function createFormationScreenRuntime(deps) {
    const {
      getCharacters,
      getEquipmentCards,
      getOwnedCount,
      getOwnedEquipmentCount,
      getCardInstances,
      getEquipmentInstances,
      getCardInstance,
      getEquipmentInstance,
      getCardInstanceGrowth,
      getEquipmentInstanceGrowth,
      getPartyFormation,
      setPartyFormation,
      getSystemConfig,
      getCharacterImageForUsage,
      getRarityLabel,
      getRarityCssClass,
      makeFallbackImage,
      showCardDetail,
      getCardGrowth,
      getEquipmentGrowth,
      getGrowthResources,
      setCardLockedCopies,
      setEquipmentLockedCopies,
      enhanceCard,
      enhanceCardInstance,
      enhanceEquipment,
      enhanceEquipmentInstance,
      evolveCard,
      evolveCardInstance,
      evolveEquipment,
      evolveEquipmentInstance,
      limitBreakCard,
      limitBreakCardInstance,
      limitBreakEquipment,
      limitBreakEquipmentInstance,
      convertCardDuplicates,
      convertEquipmentDuplicates,
      convertSelectedCharacterCards,
      convertSelectedEquipmentCards,
      convertSelectedCharacterInstances,
      convertSelectedEquipmentInstances,
      convertStaminaToGrowthPoints,
      getPlayerCurrencyAmount,
      getDefaultBattleState,
      setBattleState,
      navigateTo,
      showToast,
      esc
    } = deps;

    function setup() {
      return window.FormationScreen?.setupFormationScreen?.({
        getCharacters,
        getEquipmentCards,
        getOwnedCount,
        getOwnedEquipmentCount,
        getCardInstances,
        getEquipmentInstances,
        getCardInstance,
        getEquipmentInstance,
        getCardInstanceGrowth,
        getEquipmentInstanceGrowth,
        getPartyFormation,
        setPartyFormation,
        getSystemConfig,
        getCharacterImageForUsage,
        getRarityLabel,
        getRarityCssClass,
        makeFallbackImage,
        showCardDetail,
        getCardGrowth,
        getEquipmentGrowth,
        getGrowthResources,
        setCardLockedCopies,
        setEquipmentLockedCopies,
        enhanceCard,
        enhanceCardInstance,
        enhanceEquipment,
        enhanceEquipmentInstance,
        evolveCard,
        evolveCardInstance,
        evolveEquipment,
        evolveEquipmentInstance,
        limitBreakCard,
        limitBreakCardInstance,
        limitBreakEquipment,
        limitBreakEquipmentInstance,
        convertCardDuplicates,
        convertEquipmentDuplicates,
        convertSelectedCharacterCards,
        convertSelectedEquipmentCards,
        convertSelectedCharacterInstances,
        convertSelectedEquipmentInstances,
        convertStaminaToGrowthPoints,
        getPlayerCurrencyAmount,
        getDefaultBattleState,
        setBattleState,
        navigateTo,
        showToast,
        esc
      }) || null;
    }

    return {
      setup
    };
  }

  window.SociaFormationScreenRuntime = {
    create: createFormationScreenRuntime
  };
})();
